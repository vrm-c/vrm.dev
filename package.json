{
  "name": "vrm.dev",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "autoprefixer": "^9.6.1",
    "cross-env": "^6.0.0",
    "postcss-cli": "^6.1.3"
  },
  "scripts": {
    "build": "cross-env HUGO_ENV=production hugo --minify"
  }
}
